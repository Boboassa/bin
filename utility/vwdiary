#!/bin/sh

filename="$HOME/vimwiki/diary/$(date +%Y-%m-%d).wiki"

if [ ! -f $filename ]; then
    printf '# Notes for %s' "$(date +%Y-%m-%d)" > $filename
fi

printf '\n\n## %s\n\n' "$(date +%H:%M)" >> $filename

vim -c "norm Go" -c "startinsert" $filename
